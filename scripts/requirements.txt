scholarly>=1.7.11
free-proxy>=1.1.1
# httpx 0.28 removed the `proxies` kwarg from httpx.Client(), which scholarly
# 1.7.x still passes — pin to the last compatible release until scholarly
# updates.
httpx>=0.27,<0.28
